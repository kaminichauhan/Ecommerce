import React from 'react';
// import { Mycontext } from './ContextAPI';

const Cart = () => {
  // const data = useContext(Mycontext);
  return (
    <div>
      <input type="text"/>
    </div>
  )
};

export default Cart;
